namespace SmartCity.Api.Features.State;

public static class StateEndpoints
{
    // TODO: GET /api/state — aggregated operational dashboard data
    public static void MapStateEndpoints(this IEndpointRouteBuilder app)
    {
    }
}
